# V2.1 开发任务书 — 学习闭环 MVP

> 本文件是给 Codex 的开发指令。开始前先读 `docs/CONTEXT.md` 和 `docs/Phase2-design.md`。

---

## 当前暂停点（2026-06-01）

**分支**：`feature/v2.1-learning-loop`

**当前状态**：V2.1 学习闭环 MVP 已完成一版实现，准备提交为阶段保存点。

**已完成范围**：
- 后端：Users 模型、JWT 认证、notes 用户隔离、review_cards/review_records、SM-2 调度、Review API、卡片生成兜底逻辑
- 前端：登录/注册、路由守卫、Axios Token 注入与 refresh、间隔复习页面、Dashboard 基础版、侧边栏更新
- 数据库：新增迁移 `9a3c2f1d4b7e_add_users_notes_review_tables.py`，本地数据库已升级到该 head
- 文档：`docs/CONTEXT.md` 已同步新增 API、Schema、V2.1 checklist 和更新日志

**验证记录**：
- `D:\Python\venvs\knowbase\Scripts\python.exe -m pytest tests -q` → `6 passed`
- `D:\Python\venvs\knowbase\Scripts\python.exe -c "import app.main; import app.models; print('backend import ok')"` → `backend import ok`
- `npm run build`（frontend）→ Vite build 成功，有 chunk size 警告
- `D:\Python\venvs\knowbase\Scripts\alembic.exe current` → `9a3c2f1d4b7e (head)`
- 后端 API 冒烟：注册 → `/auth/me` → 创建知识点 → 自动生成 3 张卡片 → 提交复习评分，返回 `interval=1`

**已知注意事项**：
- `bcrypt` 已约束为 `>=4.0.0,<5.0.0`，避免 `passlib 1.7.4` 与 `bcrypt 5` 兼容问题
- 未配置 LLM API key 时，复习卡片会用本地模板兜底生成，保证闭环可运行
- 当前测试覆盖仍偏核心单元，尚未覆盖完整 API 自动化测试和前端交互测试
- 前端 build 产物提示 chunk 超过 500 kB，后续可做路由级动态导入优化

---

## 目标

跑通"创建知识点 → 自动生成复习卡片 → 间隔复习 → 掌握度提升"的完整循环。

用户注册登录后，可以：
1. 创建/编辑知识点（复用现有 notes CRUD）
2. 每个知识点自动生成 3 张复习卡片（概念/代码/场景）
3. 在复习页面翻卡片、评分
4. SM-2 算法根据评分自动调度下次复习时间
5. Dashboard 看到今日待复习数量和掌握度分布

---

## 前置条件

- 读 `docs/CONTEXT.md` 了解项目现状（Phase 1 已完成）
- 读 `docs/Phase2-design.md` 了解完整设计（重点关注第三章数据库设计和第六章 V2.1 部分）
- 先 `git status`，确认没有其他 Agent 未提交的修改

---

## 开发步骤（按顺序，每步完成后可独立验证）

### Step 1：Users 模型 + Alembic 迁移

**新建** `backend/app/models/user.py`：
- 使用 SQLAlchemy 2.0 Mapped 风格（参考 `models/note.py`）
- 字段：id (UUID PK), username (unique), email (unique, nullable), password_hash, reminder_enabled (default false), reminder_time (nullable), created_at
- 密码哈希用 `passlib[bcrypt]`（需在 pyproject.toml 添加依赖）

**修改** `backend/app/models/__init__.py`：注册 User 模型

**生成 Alembic 迁移**：`alembic revision --autogenerate -m "add users table"`

**验证**：`alembic upgrade head` 成功，`\dt` 看到 users 表

### Step 2：Auth 服务 + API + JWT 中间件

**新建** `backend/app/utils/deps.py`：
- `get_current_user(token, db)` — 解析 JWT，返回 User 对象
- 类似 Spring Security 的 `@AuthenticationPrincipal`
- token 无效时返回 401

**新建** `backend/app/services/auth_service.py`：
- `register(db, username, password)` — 创建用户，密码 bcrypt 哈希
- `login(db, username, password)` — 验证密码，返回 access_token + refresh_token
- `refresh_token(refresh_token)` — 刷新 access_token
- access_token 有效期 30 分钟，refresh_token 有效期 7 天
- JWT 密钥用 config.py 的 `APP_SECRET_KEY`

**新建** `backend/app/schemas/auth.py`：
- `RegisterRequest(username, password)`
- `LoginRequest(username, password)`
- `TokenResponse(access_token, refresh_token, token_type)`
- `UserOut(id, username, email, created_at)`

**新建** `backend/app/api/auth.py`：
- POST `/auth/register`
- POST `/auth/login`
- POST `/auth/refresh`
- GET `/auth/me`（需要认证）

**修改** `backend/app/api/router.py`：注册 auth_router

**修改** `backend/app/config.py`：确认 `APP_SECRET_KEY` 字段存在（已有）

**验证**：
1. `curl POST /api/v1/auth/register` 注册成功
2. `curl POST /api/v1/auth/login` 返回 token
3. `curl GET /api/v1/auth/me -H "Authorization: Bearer xxx"` 返回用户信息
4. 不带 token 访问 → 401

### Step 3：notes 表新增字段 + 迁移

**修改** `backend/app/models/note.py`：
- 新增 `user_id: Mapped[uuid.UUID]` — FK → users.id
- 新增 `mastery_level: Mapped[int]` — default 0
- 新增 `source_type: Mapped[str]` — default "manual"
- 新增 `source_url: Mapped[str | None]` — nullable

**生成迁移**：`alembic revision --autogenerate -m "add user_id mastery_level source_type to notes"`

**验证**：迁移成功，notes 表有新字段

### Step 4：现有 API 加 user_id 过滤

**修改** `backend/app/services/note_service.py`：
- 所有查询方法增加 `user_id` 参数过滤
- `create_note` 自动设置 `user_id`
- `get_notes` / `get_note` / `update_note` / `delete_note` 都加 user_id 过滤

**修改** `backend/app/api/notes.py`：
- 所有端点注入 `current_user = Depends(get_current_user)`
- 传递 `current_user.id` 给 service 层

**修改** `backend/app/schemas/note.py`：
- `NoteCreate` 不需要 user_id（从 token 获取）
- `NoteOut` / `NoteListItem` 保持不变

**验证**：
1. 登录获取 token
2. 创建笔记 → 笔记自动关联当前用户
3. 用另一个用户的 token → 看不到别人的笔记

### Step 5：review_cards + review_records 模型 + 迁移

**新建** `backend/app/models/review.py`：

```python
class ReviewCard(Base):
    __tablename__ = "review_cards"
    # id, note_id (FK), card_type, question, answer
    # next_review_at (nullable), ease_factor (default 2.5)
    # interval_days (default 0), review_count (default 0)
    # last_reviewed_at (nullable)
    # is_user_edited (default false), is_flagged (default false)
    # created_at

class ReviewRecord(Base):
    __tablename__ = "review_records"
    # id, card_id (FK), quality, reviewed_at
```

**修改** `backend/app/models/__init__.py`：注册 ReviewCard, ReviewRecord

**生成迁移**

**验证**：迁移成功，新表创建

### Step 6：SM-2 算法 + 复习服务

**新建** `backend/app/services/review_service.py`：

核心函数：
- `calculate_next_review(quality, ease_factor, interval_days, review_count)` — SM-2 算法
  - quality < 3：忘了，interval_days = 1，review_count 重置为 0
  - quality >= 3 且 review_count == 0：interval_days = 1
  - quality >= 3 且 review_count == 1：interval_days = 6
  - quality >= 3 且 review_count > 1：interval_days = round(interval_days * ease_factor)
  - ease_factor 更新：max(1.3, ease_factor + 0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02))
- `get_today_cards(db, user_id)` — 获取 next_review_at <= now 的卡片
- `submit_review(db, card_id, quality)` — 更新 card 的 SM-2 状态 + 创建 review_record
- `get_review_stats(db, user_id)` — 掌握度统计

**验证**：Python shell 调用 SM-2 算法，确认计算结果正确

### Step 7：LLM 客户端工厂 + AI 卡片生成

**新建** `backend/app/rag/llm.py`：
- `get_llm(provider=None)` — 根据 config 返回 LLM 客户端
- 支持 deepseek / glm / openai 三家
- 用 httpx 异步调用 OpenAI 兼容接口
- 调用日志写入 ai_call_logs 表（V2.1 先打日志，ai_call_logs 表 V2.3 再建）

**新建** `backend/app/rag/card_generator.py`：
- `generate_cards(note_title, note_content)` — 调用 LLM 生成 3 张卡片
- 返回 `[{card_type, question, answer}, ...]`
- Prompt 要求 LLM 返回 JSON 格式

**修改** `backend/app/services/review_service.py`：
- `generate_cards_for_note(db, note_id)` — 调用 card_generator → 存入 review_cards 表

**验证**：
1. Python shell 调用 `get_llm()` 返回可用客户端
2. `generate_cards("JWT 认证", "JWT 由 header, payload, signature 组成...")` 返回 3 张卡片

### Step 8：Review API

**新建** `backend/app/schemas/review.py`：
- `ReviewCardOut(id, note_id, card_type, question, answer, next_review_at, ease_factor, review_count, is_user_edited, is_flagged)`
- `ReviewSubmitRequest(card_id, quality)`
- `ReviewStats(total_cards, due_today, mastered_count, learning_count, new_count)`

**新建** `backend/app/api/review.py`：
- GET `/review/today` — 今日待复习卡片列表
- GET `/review/cards/{note_id}` — 某知识点的卡片
- POST `/review/submit` — 提交评分
- GET `/review/stats` — 统计
- POST `/review/generate/{note_id}` — 生成卡片（调用一次 AI，存库）
- PUT `/review/cards/{card_id}` — 编辑卡片
- POST `/review/cards/{card_id}/flag` — 标记"不好"

**修改** `backend/app/api/router.py`：注册 review_router

**修改** `backend/app/services/note_service.py`：
- `create_note` 完成后自动调用 `generate_cards_for_note`（后台任务）

**验证**：
1. 创建知识点 → review_cards 表自动生成 3 条记录
2. GET /review/today 返回待复习卡片
3. POST /review/submit 提交评分 → card 的 next_review_at 更新
4. PUT /review/cards/{id} 编辑卡片内容 → is_user_edited = true

### Step 9：前端 Auth

**新建** `frontend/src/store/authStore.ts`（Zustand）：
- state: user, accessToken, refreshToken, isAuthenticated
- actions: login, register, logout, refreshToken

**新建** `frontend/src/api/auth.ts`：
- login, register, refreshToken, getMe

**修改** `frontend/src/api/client.ts`：
- 请求拦截器自动带 Authorization header
- 响应拦截器 401 时自动 refresh token，失败则跳转登录

**新建** `frontend/src/pages/Login.tsx`：
- 用户名 + 密码表单（Ant Design Form）
- 登录成功跳转首页

**新建** `frontend/src/pages/Register.tsx`：
- 注册表单，注册成功自动跳转登录

**新建** `frontend/src/components/AuthRoute.tsx`：
- 路由守卫：未登录跳转 /login

**修改** `frontend/src/App.tsx`：
- /login 和 /register 不需要登录
- 其他路由包裹 AuthRoute

**验证**：浏览器注册 → 登录 → 看到首页；退出 → 跳转登录页

### Step 10：前端 Review UI

**新建** `frontend/src/api/review.ts`：
- getToday, getCards, submitReview, getStats, generateCards, editCard, flagCard

**新建** `frontend/src/pages/Review.tsx`：
- 顶部显示：今日复习 X 个，已完成 Y/X，连续 Z 天
- 卡片翻转效果（点击卡片 → 翻转显示答案）
- 三个评分按钮：完全忘了(0) / 有点印象(3) / 很熟悉(5)
- 进度条显示当前第几张

**修改** `frontend/src/components/Layout.tsx`：
- 侧边栏新增"间隔复习"菜单项

**修改** `frontend/src/App.tsx`：
- 新增 /review 路由

**验证**：浏览器进入复习页面 → 翻卡片 → 评分 → 下一张 → 完成后显示统计

### Step 11：Dashboard 基础版

**新建** `frontend/src/pages/Dashboard.tsx`：
- 今日待复习数量（大数字展示）
- 掌握度分布（Ant Design Charts 饼图：已掌握/学习中/未学）
- 最近学习的知识点列表（前 5 条）
- 连续学习天数

**修改** `frontend/src/pages/Home.tsx`：
- 用 Dashboard 替换原有的占位内容

**验证**：首页显示正确的统计数据

---

## 技术要点

### 密码哈希
```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
# 哈希：pwd_context.hash(password)
# 验证：pwd_context.verify(plain, hashed)
```

### JWT Token
```python
from jose import jwt, JWTError
# 生成：jwt.encode(payload, SECRET_KEY, algorithm="HS256")
# 解码：jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
# payload 包含：sub(user_id), exp(过期时间)
```

### FastAPI 依赖注入
```python
# deps.py
async def get_current_user(token: str = Depends(oauth2_scheme), db: AsyncSession = Depends(get_db)):
    # 解码 token → 查 user → 返回 user
    ...

# api/notes.py
@router.get("")
async def list_notes(current_user: User = Depends(get_current_user), ...):
    notes = await note_service.get_notes(db, user_id=current_user.id, ...)
```

### 前端 Zustand Store
```typescript
import { create } from 'zustand'

interface AuthState {
  user: User | null
  accessToken: string | null
  login: (username: string, password: string) => Promise<void>
  logout: () => void
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  accessToken: localStorage.getItem('access_token'),
  login: async (username, password) => {
    const res = await authApi.login(username, password)
    localStorage.setItem('access_token', res.data.access_token)
    set({ user: res.data.user, accessToken: res.data.access_token })
  },
  logout: () => {
    localStorage.removeItem('access_token')
    set({ user: null, accessToken: null })
  },
}))
```

---

## 依赖新增

在 `backend/pyproject.toml` 中添加：
```
"python-jose[cryptography]",  # JWT
"passlib[bcrypt]",             # 密码哈希
"bcrypt",                      # bcrypt 实现
```

前端在 `frontend/package.json` 中添加：
```
"zustand"    # 已声明，确认版本
```

---

## 完成后

1. 更新 `docs/CONTEXT.md` 的 V2.1 checklist
2. 更新 `docs/CONTEXT.md` 的 API 端点章节（新增 Auth + Review 端点）
3. 更新 `docs/CONTEXT.md` 的数据库 Schema 章节（新增 users / review_cards / review_records）
4. 更新 `docs/CONTEXT.md` 的更新日志
5. Commit：`feat: implement V2.1 learning loop MVP`
