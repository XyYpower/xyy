# V3.0 开发任务书 — 体验打磨 + 内容管理

> 本文件是给 Claude Code 的开发指令。开始前先读 `docs/CONTEXT.md` 了解项目全貌。
> 本版本聚焦 Phase 3：补齐内容管理短板、升级编辑体验、完善账号设置，让产品从"能跑"变成"好用"。

---

## 前置条件

- **分支**：从 `feature/v2.3-rag-interview` 创建新分支 `feature/v3.0-polish`
- **依赖**：V2.3 已完成（RAG 对话、AI 面试、数据导出）
- 先 `git status`，确认没有未提交的修改
- 确认前端依赖：`lexical`、`@lexical/*`、`react-markdown`、`rehype-highlight`、`remark-gfm` 已安装
- 确认后端 `notes` 表已有 `is_favorite`、`mastery_level`、`category_id` 字段
- 确认后端 `users` 表已有 `reminder_enabled`、`reminder_time`、`email` 字段

---

## 目标

让 KnowBase 从"功能可用"升级为"体验流畅"。具体：

1. **Markdown 编辑器** — 用 Lexical 替换原生 textarea，支持工具栏、代码高亮、实时预览
2. **分类管理** — 分类增删改 + 知识点选择分类 + 按分类筛选
3. **标签编辑** — 知识点详情页添加/移除标签
4. **收藏功能** — 收藏/取消收藏 + 收藏筛选
5. **账号设置增强** — 修改密码、更新邮箱、复习提醒开关和时间
6. **前端构建优化** — 路由级懒加载，消除 chunk size 警告

---

## 开发步骤（按顺序）

### Step 1：categories 表加 user_id + CRUD API

**问题**：当前 `categories` 表没有 `user_id`，分类是全局的，不符合用户数据隔离原则。

**修改** `backend/app/models/note.py` — Category 模型新增 user_id：

```python
class Category(Base):
    __tablename__ = "categories"

    id: Mapped[uuid.UUID] = mapped_column(UUID(as_uuid=True), primary_key=True, default=uuid.uuid4)
    user_id: Mapped[uuid.UUID] = mapped_column(  # 新增
        UUID(as_uuid=True), ForeignKey("users.id", ondelete="CASCADE"), nullable=False, index=True
    )
    name: Mapped[str] = mapped_column(String(100), nullable=False)
    description: Mapped[str | None] = mapped_column(Text, nullable=True)
    sort_order: Mapped[int] = mapped_column(Integer, default=0)
    created_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now())
    updated_at: Mapped[datetime] = mapped_column(DateTime, server_default=func.now(), onupdate=func.now())

    user: Mapped["User"] = relationship("User")  # 新增
    notes: Mapped[list["Note"]] = relationship("Note", back_populates="category")
```

**新增 Schema** `backend/app/schemas/note.py`：

```python
class CategoryCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=100)
    description: str | None = None

class CategoryUpdate(BaseModel):
    name: str | None = Field(None, min_length=1, max_length=100)
    description: str | None = None
    sort_order: int | None = None
```

**新增 service 函数** `backend/app/services/note_service.py`：

```python
async def create_category(db: AsyncSession, user_id: uuid.UUID, data: CategoryCreate) -> Category:
    cat = Category(user_id=user_id, **data.model_dump())
    db.add(cat)
    await db.flush()
    await db.refresh(cat)
    return cat

async def update_category(db: AsyncSession, user_id: uuid.UUID, cat_id: uuid.UUID, data: CategoryUpdate) -> Category:
    cat = await db.get(Category, cat_id)
    if not cat or cat.user_id != user_id:
        raise HTTPException(404, "分类不存在")
    for k, v in data.model_dump(exclude_unset=True).items():
        setattr(cat, k, v)
    await db.flush()
    await db.refresh(cat)
    return cat

async def delete_category(db: AsyncSession, user_id: uuid.UUID, cat_id: uuid.UUID) -> None:
    cat = await db.get(Category, cat_id)
    if not cat or cat.user_id != user_id:
        raise HTTPException(404, "分类不存在")
    await db.delete(cat)
    await db.flush()
```

**修改** `backend/app/api/categories.py` — 新增 CRUD 端点：

| 端点 | 说明 |
|------|------|
| GET `/categories` | 当前用户分类列表（已有，改为按 user_id 过滤） |
| POST `/categories` | 创建分类 |
| PUT `/categories/{id}` | 更新分类 |
| DELETE `/categories/{id}` | 删除分类 |

**生成迁移**：`alembic revision --autogenerate -m "add user_id to categories"`

迁移中需要处理已有分类数据：
```python
# upgrade 中手动处理：给已有分类分配给第一个用户
# op.execute("UPDATE categories SET user_id = (SELECT id FROM users ORDER BY created_at LIMIT 1) WHERE user_id IS NULL")
# 然后删除 user_id 的 nullable
```

**验证**：CRUD 端点测试通过，按用户隔离正确

### Step 2：账号设置 API

**新增** `backend/app/schemas/auth.py`：

```python
class UpdateProfileRequest(BaseModel):
    email: str | None = Field(None, max_length=100)
    reminder_enabled: bool | None = None
    reminder_time: str | None = None  # "HH:MM" 格式

class ChangePasswordRequest(BaseModel):
    old_password: str = Field(..., min_length=6)
    new_password: str = Field(..., min_length=6, max_length=128)
```

**新增** `backend/app/api/auth.py` 端点：

| 端点 | 说明 |
|------|------|
| PUT `/auth/profile` | 更新资料（邮箱、提醒设置） |
| PUT `/auth/password` | 修改密码 |

```python
@router.put("/profile")
async def update_profile(
    data: UpdateProfileRequest,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    if data.email is not None:
        current_user.email = data.email
    if data.reminder_enabled is not None:
        current_user.reminder_enabled = data.reminder_enabled
    if data.reminder_time is not None:
        current_user.reminder_time = datetime.strptime(data.reminder_time, "%H:%M").time()
    await db.flush()
    return success(UserOut.model_validate(current_user))


@router.put("/password")
async def change_password(
    data: ChangePasswordRequest,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db),
):
    if not verify_password(data.old_password, current_user.password_hash):
        raise HTTPException(400, "旧密码错误")
    current_user.password_hash = hash_password(data.new_password)
    await db.flush()
    return success(None, message="密码已修改")
```

**新增前端 API** `frontend/src/api/auth.ts`：

```typescript
export const authApi = {
  // ... 已有的 login / register / me / refresh
  updateProfile: (data: { email?: string; reminder_enabled?: boolean; reminder_time?: string }) =>
    client.put<any, ApiResponse<UserOut>>('/auth/profile', data),
  changePassword: (data: { old_password: string; new_password: string }) =>
    client.put<any, ApiResponse<null>>('/auth/password', data),
}
```

**验证**：
1. 修改邮箱 → 返回更新后的用户信息
2. 开启/关闭提醒 + 设置时间 → 数据库正确更新
3. 修改密码 → 旧密码验证失败时报错，成功后新密码可登录

### Step 3：前端 Settings 页面增强

**重写** `frontend/src/pages/Settings.tsx`：

```
┌──────────────────────────────────────────────────────────┐
│  设置                                                     │
├──────────────────────────────────────────────────────────┤
│  账号信息                                                 │
│  用户名：developer          [不可修改]                     │
│  邮箱：  [dev@example.com]               [保存]           │
│  ─────────────────────────────────────────────────────── │
│  修改密码                                                 │
│  当前密码：[__________]                                   │
│  新密码：  [__________]                                   │
│  确认密码：[__________]                    [修改密码]      │
│  ─────────────────────────────────────────────────────── │
│  复习提醒                                                 │
│  [✓] 开启每日提醒                                         │
│  提醒时间：[09:00]                        [保存]          │
│  ─────────────────────────────────────────────────────── │
│  数据导出                                                 │
│  [导出 JSON]  [导出 Markdown]  [导出 Anki]               │
└──────────────────────────────────────────────────────────┘
```

交互要点：
- 邮箱、提醒设置各自独立保存，避免误操作
- 修改密码需二次确认（新密码输入两遍）
- 提醒开关 + 时间联动（关闭时时间灰显）

### Step 4：前端分类管理 UI

**新建** 分类管理弹窗组件 `frontend/src/components/CategoryManager.tsx`：

在 Notes 列表页顶部增加分类管理入口（齿轮图标 → 弹窗）：

```
┌──────────────────────────────────────────┐
│  分类管理                        [×]     │
├──────────────────────────────────────────┤
│  ┌────────────────────────────────────┐  │
│  │ 分类名称        [编辑] [删除]      │  │
│  │ 后端开发        [编辑] [删除]      │  │
│  │ 前端框架        [编辑] [删除]      │  │
│  │ 系统设计        [编辑] [删除]      │  │
│  └────────────────────────────────────┘  │
│  [+ 新建分类]                            │
└──────────────────────────────────────────┘
```

**修改** `frontend/src/pages/Notes.tsx`：
- 顶部增加分类筛选下拉（Ant Design Select）
- 筛选联动笔记列表

**修改** `frontend/src/pages/NoteDetail.tsx`：
- 标题下方增加分类选择器（Ant Design Select）
- 保存时一并提交 category_id

**新增前端 API** `frontend/src/api/categories.ts`：

```typescript
export const categoryApi = {
  list: () => client.get<any, ApiResponse<Category[]>>('/categories'),
  create: (data: { name: string; description?: string }) =>
    client.post<any, ApiResponse<Category>>('/categories', data),
  update: (id: string, data: { name?: string; description?: string }) =>
    client.put<any, ApiResponse<Category>>(`/categories/${id}`, data),
  delete: (id: string) => client.delete<any, ApiResponse<null>>(`/categories/${id}`),
}
```

**验证**：
1. 新建分类 → 列表出现 → 刷新后仍在
2. 编辑分类名 → 知识点详情页下拉同步更新
3. 删除分类 → 确认弹窗 → 该分类下知识点 category_id 置空
4. 按分类筛选知识点 → 结果正确

### Step 5：标签编辑 UI

**修改** `frontend/src/pages/NoteDetail.tsx`：

将只读的 Tag 列表替换为可编辑标签组件：

```
┌──────────────────────────────────────────────────┐
│  标签：[后端] [JWT] [认证] [+ 添加标签]           │
│                                                   │
│  点击 [+ 添加标签] 弹出：                          │
│  ┌────────────────────────────────────────────┐  │
│  │ 搜索标签...                                │  │
│  │ [已有标签1]  [已有标签2]  [已有标签3]       │  │
│  │ ──────────────────────────────────────     │  │
│  │ 创建新标签 "xxx"                            │  │
│  └────────────────────────────────────────────┘  │
│                                                   │
│  点击标签上的 [×] 可移除                           │
└──────────────────────────────────────────────────┘
```

实现方式：
- 使用 Ant Design `Select` 组件的 `mode="tags"` 模式
- 搜索已有标签（调用 GET `/tags`）
- 输入新标签名 → 保存时通过 `tag_names` 字段自动创建
- 移除标签 → 保存时提交更新后的 `tag_names`

**验证**：
1. 添加已有标签 → 保存成功，标签显示在详情页
2. 创建新标签 → 保存后新标签出现在标签列表
3. 移除标签 → 保存后标签消失
4. 多次添加/移除不产生重复标签

### Step 6：收藏功能

**修改** `frontend/src/pages/NoteDetail.tsx`：
- 标题行右侧增加收藏按钮（实心/空心星星图标）
- 点击切换收藏状态，调用 `PUT /notes/{id}` 更新 `is_favorite`

**修改** `frontend/src/pages/Notes.tsx`：
- 卡片右上角增加收藏星星图标
- 顶部筛选区增加"只看收藏"开关
- 调用 `noteApi.list` 时传 `is_favorite=true` 参数

**后端**：`GET /notes` 已支持按字段筛选，需在 `note_service.py` 的 list 函数中增加 `is_favorite` 过滤：

```python
# 在 _apply_filters 中增加
if filters.get("is_favorite") is not None:
    stmt = stmt.where(Note.is_favorite == filters["is_favorite"])
```

**修改** `backend/app/schemas/note.py` — NoteListItem 和 NoteCreate 确认包含 is_favorite（已有）。

**修改** `backend/app/api/notes.py` — list 端点接受 `is_favorite` 查询参数。

**验证**：
1. 知识点详情页点击收藏 → 星星变实心 → 刷新保持
2. 列表页点击收藏 → 卡片状态同步
3. 筛选"只看收藏" → 只显示收藏的知识点
4. 取消收藏 → 从收藏列表中消失

### Step 7：Markdown 编辑器

**方案**：采用分屏编辑模式（左编辑 + 右预览），用原生 textarea + react-markdown 实现。
> 不使用 Lexical 的原因：Lexical 富文本编辑器配置复杂，与本项目 Markdown 存储模式不匹配。
> 分屏模式更直观，学习成本低，且 `react-markdown` + `rehype-highlight` 已安装。

**新建** `frontend/src/components/MarkdownEditor.tsx`：

```
┌──────────────────────────────────────────────────────────┐
│  [B] [I] [Code] [Link] [List] [H1] [H2] [Quote] [---]  │  ← 工具栏
├─────────────────────────────┬────────────────────────────┤
│  ## JWT 认证流程            │  JWT 认证流程               │  ← 分屏
│  JWT 由三部分组成...         │  JWT 由三部分组成...         │
│  ```python                  │  [代码块高亮显示]            │
│  import jwt                 │                             │
│  ```                        │                             │
│                             │                             │
├─────────────────────────────┴────────────────────────────┤
│  [编辑] [预览] [分屏]                    字数：234        │  ← 底部切换
└──────────────────────────────────────────────────────────┘
```

组件 Props：
```typescript
interface MarkdownEditorProps {
  value: string
  onChange: (value: string) => void
  placeholder?: string
  minHeight?: string
}
```

工具栏按钮（插入 Markdown 语法）：
- **粗体** — `**text**`
- *斜体* — `*text*`
- `代码` — `` `code` ``
- [链接](url) — `[text](url)`
- 列表 — `- item`
- H1/H2 — `# / ##`
- 引用 — `> quote`
- 分割线 — `---`
- 代码块 — ` ```\ncode\n``` `

预览区使用 `react-markdown` + `rehype-highlight` + `remark-gfm` 渲染（和 Chat 页面的 AI 回复渲染一致）。

三种视图模式：
- 编辑模式：只显示 textarea
- 预览模式：只显示渲染结果
- 分屏模式：左右各一半（默认）

**修改** `frontend/src/pages/NoteDetail.tsx`：
- 替换 `<textarea>` 为 `<MarkdownEditor>`
- 保留标题编辑、标签、保存按钮不变

**验证**：
1. 工具栏按钮点击 → 正确插入 Markdown 语法
2. 分屏模式下实时预览渲染正确
3. 代码块语法高亮正常
4. 保存后重新加载内容不丢失
5. 编辑模式切换流畅

### Step 8：前端构建优化（路由懒加载）

**问题**：当前所有页面同步导入，主 chunk 约 1.5MB，Vite 构建有 chunk size 警告。

**修改** `frontend/src/App.tsx` — 改用 `React.lazy` + `Suspense`：

```typescript
import { lazy, Suspense } from 'react'
import { Spin } from 'antd'

const Home = lazy(() => import('./pages/Home'))
const Notes = lazy(() => import('./pages/Notes'))
const NoteDetail = lazy(() => import('./pages/NoteDetail'))
const Chat = lazy(() => import('./pages/Chat'))
const Review = lazy(() => import('./pages/Review'))
const Interview = lazy(() => import('./pages/Interview'))
const Import = lazy(() => import('./pages/Import'))
const Paths = lazy(() => import('./pages/Paths'))
const Settings = lazy(() => import('./pages/Settings'))
// Login / Register 保持同步加载（首屏需要）

function App() {
  return (
    <Routes>
      <Route path="/login" element={<Login />} />
      <Route path="/register" element={<Register />} />
      <Route element={<AuthRoute />}>
        <Route path="/" element={<Layout />}>
          <Route index element={
            <Suspense fallback={<div className="flex justify-center p-8"><Spin size="large" /></div>}>
              <Home />
            </Suspense>
          } />
          {/* ... 其他路由同理 */}
        </Route>
      </Route>
    </Routes>
  )
}
```

**验证**：
1. `npm run build` 无 chunk size 警告
2. 各页面路由切换正常，显示加载中状态
3. 首次加载 Login 页面不加载其他页面代码

### Step 9：NoteDetail 页面整合升级

**综合修改** `frontend/src/pages/NoteDetail.tsx`：

将 Step 4-7 的改动整合到一个完整的知识点编辑页面：

```
┌──────────────────────────────────────────────────────────┐
│  [← 返回]    JWT 认证流程详解               [★] [保存]   │
├──────────────────────────────────────────────────────────┤
│  分类：[后端开发 ▼]    掌握度：[学习中 ▼]                 │
│  标签：[JWT] [认证] [+ 添加]                             │
├──────────────────────────────────────────────────────────┤
│  ┌─────────────── MarkdownEditor ──────────────────────┐ │
│  │  工具栏：[B] [I] [Code] [Link] [H1] [List] [Quote] │ │
│  ├─────────────────────┬───────────────────────────────┤ │
│  │  编辑区             │  预览区                        │ │
│  └─────────────────────┴───────────────────────────────┘ │
├──────────────────────────────────────────────────────────┤
│  来源：手动创建 | 创建于 2026-06-01 | 更新于 2026-06-02  │
└──────────────────────────────────────────────────────────┘
```

功能清单：
- 分类下拉选择（从 categories API 加载）
- 掌握度选择（下拉：未学/学习中/已掌握）
- 标签编辑（Select mode="tags"）
- 收藏按钮
- Markdown 编辑器
- 来源和日期信息（只读显示）
- 保存按钮提交全部字段

**修改** `frontend/src/api/notes.ts` — update 方法支持更多字段：

```typescript
update: (id: string, data: Partial<{
  title: string
  content: string
  tag_names: string[]
  category_id: string | null
  is_favorite: boolean
  mastery_level: number
}>) => client.put<any, ApiResponse<Note>>(`/notes/${id}`, data),
```

### Step 10：集成验证 + 文档更新

**验证完整流程**：
1. 登录 → 设置页 → 修改邮箱 → 保存成功
2. 设置页 → 修改密码 → 登出 → 新密码登录成功
3. 设置页 → 开启提醒 + 设置时间 → 保存成功
4. 知识点列表 → 分类筛选 → 结果正确
5. 知识点列表 → 收藏筛选 → 结果正确
6. 分类管理 → 新建/编辑/删除 → 操作正确
7. 知识点详情 → 编辑分类/标签/掌握度/收藏 → 保存成功
8. 知识点详情 → Markdown 编辑器工具栏 + 分屏预览 → 渲染正确
9. `npm run build` → 无 chunk size 警告
10. 后端测试全部通过

**更新文档**：
- `docs/CONTEXT.md`：Phase 3 checklist 打勾 + 新增 API 端点 + 更新日志
- `docs/CONTEXT.md`：已知问题中移除已修复的条目

---

## 技术要点

### Markdown 编辑器分屏实现

```typescript
function MarkdownEditor({ value, onChange, placeholder, minHeight = '60vh' }: MarkdownEditorProps) {
  const [mode, setMode] = useState<'edit' | 'preview' | 'split'>('split')

  const insertMarkdown = (before: string, after: string = '') => {
    const textarea = textareaRef.current
    if (!textarea) return
    const start = textarea.selectionStart
    const end = textarea.selectionEnd
    const selected = value.substring(start, end)
    const newValue = value.substring(0, start) + before + selected + after + value.substring(end)
    onChange(newValue)
    // 恢复光标位置
    setTimeout(() => {
      textarea.selectionStart = start + before.length
      textarea.selectionEnd = start + before.length + selected.length
      textarea.focus()
    }, 0)
  }

  return (
    <div>
      <Toolbar onInsert={insertMarkdown} />
      <div style={{ minHeight }} className="flex">
        {(mode === 'edit' || mode === 'split') && (
          <textarea value={value} onChange={e => onChange(e.target.value)} />
        )}
        {(mode === 'preview' || mode === 'split') && (
          <ReactMarkdown rehypePlugins={[rehypeHighlight]} remarkPlugins={[remarkGfm]}>
            {value}
          </ReactMarkdown>
        )}
      </div>
      <ViewModeSwitch value={mode} onChange={setMode} />
    </div>
  )
}
```

### categories 数据迁移策略

已有分类无 user_id，迁移时需：
1. 先新增 user_id 列（nullable=True）
2. 用 SQL 将已有分类分配给最早用户
3. 再将 user_id 改为 NOT NULL

```python
def upgrade():
    op.add_column("categories", sa.Column("user_id", UUID(as_uuid=True), nullable=True))
    op.execute("UPDATE categories SET user_id = (SELECT id FROM users ORDER BY created_at LIMIT 1) WHERE user_id IS NULL")
    op.alter_column("categories", "user_id", nullable=False)
    op.create_foreign_key("fk_categories_user_id", "categories", "users", ["user_id"], ["id"], ondelete="CASCADE")
    op.create_index("ix_categories_user_id", "categories", ["user_id"])
```

### 路由懒加载打包效果

改造后预期 chunk 分布：
- `vendor.js` — React + Ant Design + Axios (~800KB)
- `pages-Home-xxx.js` — Dashboard 页面
- `pages-Chat-xxx.js` — Chat + react-markdown
- `pages-Interview-xxx.js` — Interview
- `pages-Import-xxx.js` — Import
- `pages-Paths-xxx.js` — Paths
- 首屏只加载 Login + Layout + vendor，约 1MB 以内

---

## 依赖变更

本版本**不新增**后端依赖。

前端**不新增**依赖（所有需要的包已安装）：
- `react-markdown` + `rehype-highlight` + `remark-gfm` — Markdown 预览
- `@ant-design/icons` — 收藏星星等图标

---

## 已知问题处理

以下 CONTEXT.md 中的已知问题在本版本中修复：

| # | 问题 | 处理 |
|---|------|------|
| 1 | 编辑器简陋 — 原生 textarea | → Step 7 Markdown 编辑器 |
| 3 | App.css 残留 | → Step 8 清理 |
| 4 | 分类/标签管理不完整 | → Step 4-5 完整管理 UI |
| 5 | 测试覆盖少 | → 后端新 API 补充测试 |
| 13 | 前端构建体积偏大 | → Step 8 路由懒加载 |

---

## 完成后

1. 更新 `docs/CONTEXT.md` 的 Phase 3 checklist
2. 更新 `docs/CONTEXT.md` 的 API 端点章节（新增 categories CRUD + auth profile/password）
3. 更新 `docs/CONTEXT.md` 的更新日志
4. 更新 `docs/CONTEXT.md` 的已知问题（移除已修复的）
5. Commit：`feat: implement V3.0 editor, category/tag/favorite management, and settings`
